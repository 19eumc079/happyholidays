part of styles;

class BoxBorders {
  static final BorderRadius primaryBoxBorders = BorderRadius.circular(50);
  static final BorderRadius secondaryBoxBorders = BorderRadius.circular(40);
  static final BorderRadius teritaryBoxBorders = BorderRadius.circular(30);
}
